{
  "name": "oss-repo-finder",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "test": "npm --prefix frontend test -- --run && npm --prefix backend test -- --run",
    "format": "npm run format --prefix frontend && npm run format --prefix backend",
    "format:check": "npm run format:check --prefix frontend && npm run format:check --prefix backend",
    "lint": "npm run lint --prefix frontend && npm run lint --prefix backend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "ci": "npm run format && npm run format:check && npm run lint && npm test && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.0"
  }
}
